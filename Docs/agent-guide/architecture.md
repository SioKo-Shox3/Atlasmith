# Atlasmith — アーキテクチャ

<!-- 書くのは「エージェントが編集を誤らないために必要な構造知識」であり、網羅的な設計書ではない。
     コードが正、文書は補助。 -->

> **状態: 構想段階(2026-07-10 オンボード時点)。** リポジトリは LICENSE(Apache-2.0)のみで
> ソースコードは未着地。実装言語は **Python** に決定済み(2026-07-10 ユーザー確認)。
> 目的・スコープは未確定。**最初の実装フェーズで本文書の各節を実態で確定させること。**
> それまで「初期方針」の記述を既成事実として設計判断に使わない — 選定は
> [technique-selection.md](technique-selection.md) の記録+ユーザー承認を通す。

## 全体構造

未確立。スキャフォールド時の初期方針(ユーザー承認で確定させ、確定後この節を書き直す):

- `src/atlasmith/` — パッケージ本体(src レイアウト)
- `tests/` — pytest テスト(src とミラー構成)
- `Docs/` — 文書(本ガイドを含む)
- `pyproject.toml` — プロジェクト定義の単一ソース(ビルド・依存・ツール設定を集約)

## 所有権と寿命

未確立。長寿命リソース(プロセス・ファイルハンドル・外部接続・キャッシュ)を導入する際に、
生成/破棄の正規ルートと禁止パターンをここに記録する。

## スレッド / 並行性

未確立。並行処理(threading / asyncio / multiprocessing)の採用は technique-selection.md に
記録して決定し、決定後にここへ前提(どのデータがどの実行文脈に属すか)を書く。

## 依存方向(越えてはいけない境界)

未確立。最初のモジュール分割時に「X は Y を import しない」を具体名で記録する。

## 危険地帯(変更時に必ず計画レビューを通す領域)

コード未着地の現段階での高リスク変更は次のとおり(orchestration.md の昇格条件と対応):

1. **技術選定・依存追加**(`pyproject.toml` への依存追加、フレームワーク採用)—
   technique-selection.md の記録+ユーザー承認が必須。
2. **プロジェクト構造・公開 API の初期確定** — 後から動かすコストが大きい。
3. **ライセンス境界** — Apache-2.0 と非互換な依存(GPL 系等)を持ち込まない。
4. **ワークフロー資産**(CLAUDE.md / AGENTS.md、`.claude/`、`.codex/`、`Docs/agent-guide/`)—
   ミラー同一性と Codex フックの trust(→ codex-delegation.md「運用の落とし穴」)を壊しやすい。

コードが着地したら、この節を実際の危険地帯(公開 API・寿命管理・スレッド境界)で引き直す。
