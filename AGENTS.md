# CLAUDE.md / AGENTS.md — Atlasmith working agreement

このファイルは毎セッション読み込まれる「働き方の合意」であり、仕様書でもナレッジ集でもない。
**短く保つ。** プロジェクトの詳細な知識・規約は `Docs/agent-guide/` に置き、ここからは参照だけする。
計画が誤った前提を露呈したら、次のセッションが継承できるよう **ここか Docs を** 直す。

> **`CLAUDE.md` と `AGENTS.md` は完全に同一内容で運用する。** Claude Code は `CLAUDE.md`、
> Codex は `AGENTS.md` を読むが、中身は常に揃える。一方を編集したら必ずもう一方へ同じ変更を
> 反映すること。この2ファイル(+そこから参照される Docs)が唯一の一次情報。
> Claude メイン時は乖離をミラーガード(PostToolUse フック)が検知する。**Codex メイン時に
> フックは無い** — 編集の完了条件として直後に `cp` + `diff` で自分でミラーすること。

## 言語

- **日本語:** ユーザーとの会話(説明・計画・レビュー・リスク・質問・提案)と **コミットメッセージ**。英語でプロンプトされても日本語で返す。
- **英語:** コード、識別子、API/型名、コマンド、ファイルパス、エラーメッセージ。技術用語を無理に訳さない。

## プロジェクト概要

Atlasmith は**構想段階**のプロジェクト(2026-07-10 時点)— 目的・スコープは未確定。
実装言語は **Python** に決定済み(ruff / pytest を採用予定)。ライセンスは Apache-2.0。
構想が確定したら `Docs/agent-guide/architecture.md` に反映し、この節を書き直す。

## 絶対規則(non-negotiables)

1. **依存・技術選定はユーザー承認を経る** — 言語(Python)以外の言語・フレームワーク・
   依存パッケージの導入は、技術選定記録(`Docs/agent-guide/technique-selection.md` の雛形)+
   ユーザー承認なしに行わない。
2. **秘密情報(トークン・鍵・接続文字列)をコミットしない。**
3. **Apache-2.0 と非互換な依存(GPL 系等)を持ち込まない。**
4. **生成物をコミットしない**(`.venv/`、`__pycache__/` 等 — 一覧は `Docs/agent-guide/build-and-verify.md`)。

## ワークフロー(すべての非自明な変更はこう進む)

**メイン(オーケストレーター)= ユーザーが起動した側。** Claude Code は本書を `CLAUDE.md` として、
Codex は `AGENTS.md` として読む — 中身は同一なので、どちらをメインにしても**切り替え操作は不要**。
本書の「メイン」「パートナーAI(非メイン側)」は役割相対で読むこと。

メインセッションは **オーケストレーター** である。工程分割・設計判断・順序付け・統合・検証実行・
コミット境界管理・最終受け入れだけを行い、**具体的な計画作成・ファイル編集・自分が監督した実装の
レビューは行わない** — それらはサブエージェント(Claude メイン時は `.claude/agents/`、
Codex メイン時は `.codex/agents/`)とパートナーAIに割り当てる。

1. **調査** — `researcher`(read-only)。要約だけ受け取り、生の探索結果をメイン文脈に入れない。
2. **計画** — `planner`(最上位モデル)。
3. **計画レビュー** — メイン側 `plan-reviewer`(planner と別、最上位)一次 + **非メイン側 AI の二次(必須、直接 CLI)**。
4. **ユーザー承認** — コードを書く前に計画を提示(自律実行指示時は提示のみで続行)。
5. **テスト設計** — テストを伴うフェーズでは `test-designer`(最上位)が実装前に設計。
6. **実装** — タスクの形で振り分ける(メイン自身は書かない): **仕様が確定した実装は Codex 直接 CLI**(指示追従・コーディング品質)、**曖昧さ・横断判断が残る実装は `implementer`**(難所は上位モデルで起動)。振り分け基準と弱点相殺: `Docs/agent-guide/model-playbook.md`。
7. **実装レビュー** — メイン側 `impl-reviewer`(実装者と別、最上位)一次 + クリーン文脈の**非メイン側 AI の二次(必須ゲート)**。二次は**直接 CLI**で呼ぶ — Claude メインなら `codex exec --sandbox read-only`(同期・ポーリング不要)、Codex メインなら `claude -p`。**プラグイン経由にしない**(→ `Docs/agent-guide/codex-delegation.md`)。
8. **統合・検証・コミット** — オーケストレーターが diff を検査し、`verifier` でゲートを実行してからコミット。

**証拠を示す。断言しない。** 実行したコマンドと実際の出力・diff を貼る。
**行き詰まったら相談する。** 同一フェーズで反証・手戻りが2回続いたら、回数を重ねる前に
パートナーAI(非メイン側)へ構造化ブリーフで相談する(→ `Docs/agent-guide/codex-delegation.md` の相談プロトコル)。
手順の詳細・計画/レビューの必須チェック項目: `Docs/agent-guide/orchestration.md`。

## パートナーAI連携(Codex ⇄ Claude、相互ヘルプ)

実装はメイン側で完結させるが、**レビューの二次と相談は必ずパートナーAI(非メイン側)で行う**。
ベンダーの異なる視点によるダブルチェックは品質保証の柱であり、省略しない。

- **呼び出しは直接 CLI(プラグイン非依存):** Claude メイン → Bash から `codex exec --sandbox read-only ...`(同期実行なのでポーリング不要)。Codex メイン → `claude -p ... --permission-mode plan`。コマンドの詳細と雛形: `Docs/agent-guide/codex-delegation.md`。
- **既知の干渉:** Agent ツールをフックするプラグイン(context-mode 等)は、プラグイン経由の呼び出し(`codex:rescue` のサブエージェント)をブロックすることがある。直接 CLI はこの影響を受けない。
- **自己レビュー禁止:** 実装した本人に自分の変更をレビューさせない。二次レビューは常に非メイン側 AI。受け取った指摘・助言は「提案物」で、採否はメインが判断する。
- **フォールバック:** 直接 CLI の呼び出しが失敗した場合のみ、メイン側の独立二段(`impl-reviewer` 一次 + `verifier` 反証)で代替し、**パートナー二次を省略した旨を必ずユーザーへ報告する**(黙って省略しない)。

## モデル方針(モデル非依存)

品質保証(計画・テスト設計・計画レビュー・実装レビュー・検証)は **必ずその時点の最上位モデル**。
ただし **バージョン付きモデルIDをリポジトリに書かない** — ラインナップは変わる。

- サブエージェント定義: 品質役 = `model: inherit`(メインセッションと同モデル)、作業役 = `sonnet` / `haiku` エイリアス(各系列の最新に自動解決)。
- **メインセッションを常に利用可能な最上位で運転する**(`/model` で選択)。品質役は inherit で自動追従するため、モデルが世代交代してもこのファイルとエージェント定義の更新は不要。
- 例外: メインを節約モデルで走らせる場合は、品質役の呼び出し時に最上位エイリアスを明示指定する(品質を静かに下げない)。
- 調査・機械的作業は最安〜下位。難所は昇格。
- **品質はモデルの地力だけに頼らない** — 二重レビュー・反証・セルフチェック・多視点分解で構造的に担保する(→ `Docs/agent-guide/orchestration.md`「モデル非依存の品質戦略」)。

## ハーネス強制(文書ではなくフックが守る — Claude がメインのときのみ作動)

- `.claude/hooks/enforce-codex-impl.mjs`(PreToolUse)がメインスレッドの実装ソース `Edit`/`Write` を物理的にブロックする。
- 一時解除(ユーザー承認必須・1セッション限り): `ATLASMITH_ALLOW_DIRECT_EDIT=1` で再起動。
- `.claude/settings.local.json` はマシンローカル。別マシンでは `claude-workflow-template/deploy.ps1` で再展開。

## トークン規律

大きなファイルを丸読みしない。広い読み込みはサブエージェントへ。メインスレッドは判断と統合に温存。
`/compact` はマイルストーン境界で(計画・Done条件・未決事項を保持)。

## 品質ゲートとコミット

- ゲートのコマンドと「Done」の条件: `Docs/agent-guide/build-and-verify.md`。変更に関係するゲートだけ実行し、どれを実行したか明示する。
- コミット: 1コミット=1論理変更、日本語・命令形、対象と意図がわかる件名。詳細: `Docs/agent-guide/branching-and-commits.md`(なければ本節のみ)。

## 参照マップ(`Docs/agent-guide/`)

作業するレイヤー/工程に該当する文書を、作業前に読む:

| 文書 | 内容 |
|---|---|
| `architecture.md` | 全体構造・所有権/寿命・スレッド・依存方向・危険地帯 |
| `coding-style.md` | スタイル・命名・型規約・行末/エンコーディング |
| `build-and-verify.md` | ビルド/テスト/品質ゲートのコマンドと証拠の基準 |
| `orchestration.md` | ワークフロー詳細・計画/レビューのチェックリスト・モデル選定 |
| `codex-delegation.md` | Codex への引き継ぎテンプレートと運用の落とし穴 |
| `model-playbook.md` | モデル特性の観察記録・役割ルーティング・弱点相殺ルール |

| `technique-selection.md` | 技術選定の記入用テンプレート(選定のたびにコピーして記録) |
