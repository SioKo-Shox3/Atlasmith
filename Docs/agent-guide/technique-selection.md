# 技術選定記録: <トピック>

<!-- この文書は記入用テンプレート。技術選定のたびにコピーして
     Docs/agent-guide/technique-<トピック>.md として埋める(この原本は空欄のまま残す)。 -->

日付: <日付> ／ 状態: 調査中 | 決定済み ／ 決定者: ユーザー承認 <承認発言の引用>

## 課題定義
- 解こうとしている問題:
- 制約(技術・規約・予算):
- 成功基準(何が満たされれば成功か):

## 候補
(必ず「何もしない/今はやらない」を候補に含める)

| 候補 | 概要 | 主な根拠 | 主な懸念 |
|---|---|---|---|

## 証拠等級付き比較

各セルの主張には等級必須: 【実測】(当環境での実出力)/【外部】(公開一次資料+URL)/
【ベンダー主張】(割引いて読む)/【推測】(明記の上でなら可)。等級のない主張は表に載せない。

調査のモデル割当(model-playbook準拠): 外部調査(Web・論文・API仕様)= Codex(`codex exec`検索込み)
既定/リポジトリ内調査(実測・構造把握)= Claudeサブエージェント既定(検索が必要なら必須指定)/
load-bearingな結論は両系で独立に裏取りして突き合わせる。

## プロトタイプコスト
- 本命候補を安く検証する方法(1日スパイク等)と、その判定基準:

## Advisor往復
- 相談日時・方向・要旨(反例/リスクの指摘と対応):

## 決定記録
- 採用: (+理由)
- 却下: (各候補+理由)
- **再評価トリガー**: どうなったらこの決定を見直すか(具体条件で。例: GPU世代交代、
  候補Xのvz.y リリース、フレーム予算の変更)
